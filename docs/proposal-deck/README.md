# Proposal deck (pt-BR)

Source of the 12-slide proposal presented in September 2026: one HTML file
per slide in `slides/`, order and fonts in `deck.json`. Speaker notes are in
the `<aside>` at the end of each `<section>`.
